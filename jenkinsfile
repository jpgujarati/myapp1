pipeline {
    agent {label 'master'}
    stages {
        stage('build') {
            steps {
                echo '123 test'
                
                sh 'pwd'
                sh 'ls -l'
            }
        }
    }
}
