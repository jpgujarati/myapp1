pipeline {
    agent {label 'master'}
    stages {
        stage('build') {
            steps {
                echo '123 test'
                echo 'test1'
                sh 'pwd'
                sh 'ls -l'
            }
        }
    }
}
