pipeline {
    agent {label 'master'}
    stages {
        stage('build') {
            steps {
                echo '123 test'
                echo 'test5'
                sh 'pwd'
                sh 'ls -l'
            }
        }
    }
}
